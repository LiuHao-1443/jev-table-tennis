# 实测记录：一个「零本地算法」评测仪是怎么调出来的

[English README](README.md) · [中文 README](README.zh-CN.md)

> 这是开发过程中的完整测量日志，对应于仓库里的 `index.html` / `server.py`。
> 想看项目本身请回到 [README](README.md)。
>
> 里面每一条结论都有对应的命令和数字，包括**被否掉的方案**（更细的分档、TLS 会话复用、
> 请求流水线、换代理节点）——它们和采纳的方案一样有价值。

网页乒乓球小游戏：**你在左边（人类），JEV 在右边**。
JEV 的**每一步走位、每一个落点、每一次发球，都由 TypeSafe 的 Jev（System One 决策模型）实时决定**。

默认的 `pure` 模式里，本地只有三样东西：**一次查表 + 四行伺服 + 台面物理**。
"球会飞到哪里"这件活是它自己算的——本地不给答案（详见下一节的两种模式与实测）。

```
你在左边                         server.py（中继）                    TypeSafe Jev
   │  POST /jev/decide  ───────▶  台面数值 → 英文情境描述  ───────▶  POST /v1/systemone
   │  ◀── {move_to, speed, …} ◀── 标签 → 它自己选的那个数   ◀───────  {answers: {place}}
   └─ 伺服：拍面中心朝 move_to 移动（掉线即停）
```

## 两种模式：你要的是"验证它的能力"还是"好玩"

```bash
python3 server.py                  # 默认 pure：纯驱动，零本地算法
python3 server.py --mode assisted  # 对照：本地替它算好一些东西（为了好玩调过）
```

| | `pure`（默认）| `assisted`（对照）|
|---|---|---|
| 提示词里的「还有约 X 秒到」 | **没有** | 有（本地替它解的物理题）|
| 问法 | 一次 1 个问题：**"拍子中心放哪个位置"**，选项 = 7 个真实可达的拍位 | 3 个问题：球落在哪个区间 / 打哪个方向 / 打多重 |
| 它选完之后的本地动作 | **查表**（标签 → 它自己选的那个数，恒等）| 取区间中点、`aim→±0.45×半拍高`、`power→0.45+0.55p` 三处换算 |
| 球速上限 | **固定 551 px/s**（常数，不随实测延迟变）| 跟着实测延迟走（它越慢球越慢）|
| 本地还剩 | 物理 + 查表 + 伺服 | 同上，外加那三处换算 |

**同一批 10 个冻结球、同一判分标准（拍心误差 ≤28px）的实测**：

| 条件 | 接住 | 平均误差 | 本地替它做了什么 |
|---|---|---|---|
| assisted（给提示 + 问区间 + 取中点）| **6/10 (60%)** | 53.2px | 算了到达时间 + 取了中点 |
| 去提示，仍问区间 + 取中点 | 4/10 (40%) | 75.8px | 只取中点 |
| 去提示，改问 40 个细区间 | 0/10 | 101.3px | 取中点（但太细，它做不了）|
| **pure（去提示 + 直接选拍位）** | 4/10 (40%) | **44.3px** | **无** |

三条结论：

1. **「还有几秒到」这一句 = +20 个百分点**（60% vs 40%）。它是目前最大的一处本地辅助，pure 模式删掉了。
2. **区间中点只是二阶影响**：同样 40% 命中，pure 的平均误差 44.3px，比取中点的 75.8px 更小——因为直接问"拍子放哪"就是它真正要做的那个决定。
3. **分辨率不该为了"减少取整误差"而调细**：40 个区间让它掉到 0/10（自报置信度只剩 7%）。它的绝对精度约 50–100px，而拍高 112px，**7 档正好**。

代价要说清楚：**去掉本地辅助后它会明显变笨（10 个球接 4 个）**。那就是它的真实水平——pure 模式存在的意义正是把这个数字量出来。

**API 不支持数字输出**：`{"type":"number"}` / `{"type":"numeric"}` 一律返回 `400 Invalid request`。
所以"让它直接吐像素坐标"这条路是封死的，离散化不可避免；pure 模式的做法是让**选项本身就等于真实可执行的量**，于是本地只剩下一次查表。

## 快速开始

```bash
cp jev.config.example.json jev.config.json    # 填入你的 TypeSafe key
python3 server.py                             # 启动中继（同时托管网页）
# 打开 http://127.0.0.1:8760/
```

也可以直接双击 `index.html`（file:// 方式）打开游戏，只要中继在 `http://127.0.0.1:8760` 跑着。
只有 Python 标准库，没有依赖；不需要 node、不需要 pip install。

## Jev 是什么，以及为什么接口长得不像 chat

Jev 是 **System One 决策模型**：它不吃 `messages`，而是接收一段 `state` 情境描述 + 一组**类型化问题**
（`choice` 多选一 / `noul` 概率），返回带概率分布的答案：

```jsonc
// 请求
{"model": "jev-latest", "state": "Table tennis, top-down view. …",
 "questions": {
   "band":  {"type": "choice", "instructions": "球到拍面时在哪个 y 区间？", "criteria": {"b0": "y 152-206", …}},
   "aim":   {"type": "choice", "instructions": "回球往哪打？", "criteria": {"up": …, "flat": …, "down": …}},
   "power": {"type": "noul",   "instructions": "这板要不要发力？"}}}

// 响应
{"model": "jev-1.13.0", "answers": {
   "band": {"choice": "b4", "confidence": 0.26, "probabilities": {"b4": 0.37, "b3": 0.28, …}},
   "aim": {"choice": "down", "confidence": 0.48, …},
   "power": {"noul": 0.36}},
 "usage": {"input_tokens": 996, "output_tokens": 126}}
```

中继把答案解码成机械臂指令：区间标签 → 该区间的中心像素（`move_to`）、
`up/flat/down` → 拍面让开 ±0.45 个半拍高（撞击点偏移就是出球角度）、`noul` → 速度。
**挑哪个标签完全是 Jev 的决定，本地只做标签到像素的换算。**

## 三条实测结论（它们直接决定了这个游戏的设计）

用已知标准答案的球去考同一个模型，`python3 jev_probe.py` / `jev_probe_ask.py` 可复现：

**① 别让它做多步心算。** 同一个球，问法只差一句「球到你还剩约 0.41 秒」：

| 状态描述 | 落点命中率 | 平均置信度 |
| --- | --- | --- |
| 只给位置和速度（要它自己算除法→位移→折叠） | 50% | 0.22 |
| 补上「还剩几秒」（只剩一次乘法） | **88%** | 0.44 |

现在 `describe()` 会把飞行时间、撞边反弹规则、拍面高度、对手位置都写清楚——把测量交给代码，把判断留给模型。

**② 延迟的大头曾经根本不是模型，是中继的 TLS 握手。**

把一次调用按阶段拆开（`python3 /tmp/jev_latency_breakdown.py` 可复现）：

| 阶段 | 耗时 |
|---|---|
| DNS + TCP（本地走代理）| 2~13ms |
| **TLS 握手（每次决策都重做一遍）** | **850~2400ms** |
| **模型本身思考（请求发出 → 首字节）** | **~350ms** |

同一批请求对照：

| | 每次决策 |
|---|---|
| 每次都新建连接 | 中位 **2100ms**（1283~3538）|
| **复用同一条连接** | 第 1 次 3495ms（含握手），之后稳态 **342ms** |

**差 6 倍。** 所以 `call_jev()` 现在维持一条 keep-alive 长连接（断了自动重连一次）。
修完之后，中继口径的实测延迟：**中位 356ms**（321~550，第 1 次 1231ms 含握手）。

**③ 问题越多越慢。**（在修好连接复用之后重测，问题数的影响依然存在）

| 一次调用问几个问题 | 延迟中位 | 输入 token |
| --- | --- | --- |
| 5 个 | 3.12s | 1214 |
| 3 个（assisted）| 2.42s | 996 |
| 1 个（pure）| 1.88s | 845 |

精简状态文本没有用——实测反而更慢、更不准。

**④ 延迟必须由球速来吸收。** 修好之前 Jev 一次决策 1.2~8.9 秒（方差极大），而一板球原本只有 0.8 秒。
所以球速不是常数，而是跟着实测延迟走：

```
球速上限 = 台宽 / (延迟滑动平均 + 0.9s)
```

延迟 2.4s → 球速上限约 292 px/s → 球飞 3.3 秒，模型刚好来得及。
**这是环境约束（把球放慢，给大脑留出思考时间），不是替它做决定**——HUD 上实时显示
`延迟 0.4s · 球速上限 551 px/s`，你能看见这台机器被自己的大脑拖慢了多少。

## 看它到底是怎么想的：JEV 决策日志

右下角有个 **`▤ JEV 决策日志`** 按钮（或按 `L` 键），点一下就能看到 JEV 每一次计算的过程。
打开时比赛会自动暂停，关掉自动继续。

每条记录一行摘要，点开就是这一次的**完整过程**：

```
#37  1407ms  996 tok · $0.000042  b4 20%   x→y=367 速度 603 方向 down 发力 40%
16:14:22 · 第 1 局 5:3 · 回合 6 · 输入 1296 字 / 3 问
   ├ 【发出去的状态描述】   ← 它看到的每一个字（球的位置速度、飞行时间、撞边规则、拍面高度、对手站位、比分、机械臂热度）
   ├ 【问它的问题】         ← band / aim / power 三个问题的完整定义与选项
   ├ 【它答的原始结果】     ← choice / confidence / probabilities 原样
   ├ 【概率分布】           ← b0 5% b1 5% b2 13% b3 28% b4 32% b5 19% b6 …
   ├ 【解码成机械臂指令】   ← move_to 367 / speed 603 / recover_to 367 / note
   ├ 【成本】996 输入 token × $42/Btok = $0.00004183
   └ 【耗时】1407ms（模型 jev-1.13.0）
```

球真到达拍面之后，这条记录还会**回填真实结果**：`实际 y=337 ✅` 或 `❌ 差 41px`——
也就是它每一次判断到底准不准。面板顶部是本场汇总：`37 条 · 平均 2404ms · 共 36990 tok · $0.00155 · 落点判断 19/25`。
调用失败（限流、超时）也会留一条红条，写明错误原因。

服务端同时留了完整全场历史（刷新页面也不丢）：

```bash
curl -s "http://127.0.0.1:8760/jev/log?n=20" | python3 -m json.tool | head -40
```

## 一板球问多次（闭环修正）

延迟从 2 秒降到 0.4 秒之后，一板球（飞 ~2.9 秒）够问 **5~7 次**。
它不再是"开局赌一次"，而是随着球飞近不断修正判断——**越近判得越准**。

| | |
|---|---|
| 什么时候再问一次 | 上一次决策回来后，若「剩余飞行时间 > 预计延迟 + 0.12s」就问下一次 |
| 什么时候停 | 再问也来不及了（剩余时间不够一次往返），或者球已经不在飞向它 |
| `K` 键 | 关掉就回到「一板一判」（HUD 显示 `单判`），方便对照 |
| HUD | `延迟 0.4s ×5` = 这板球已经问了 5 次 |
| 决策日志 | 同一板球的多条记录会标「本板第 N 判」 |

**这里没有任何本地判断**：本地只决定"还能不能问"，不问"该打哪里"；
每一问都是把**当时的真实球况**（球当前的位置和速度）原样给它，让它重新算一遍。
最终拍位听**最后一次**决策的——实测：首判说 y=401、末判说 y=204，球到拍面时它真在 204。

```bash
jsc /tmp/jev_multiround.js     # 闭环测试：一板问几次、修正是谁说了算、K 键开关
```

## 让一个房间里的人一起玩

服务默认监听 `0.0.0.0`，局域网里任何人都能打开：

```
本机：      http://127.0.0.1:8760/
同一个房间：http://<你的局域网IP>:8760/   ← 启动时会打印出来，把这行发给别人就行
```

只想自己玩（不让局域网访问）：

```bash
python3 server.py --host 127.0.0.1
```

**页面不用为每个人单独改**：客户端的默认中继地址是 `location.origin`，
所以别人用你的 IP 打开页面时，它会自动连**你的** IP（而不是他自己机器上的 127.0.0.1）。
从文件直接打开（`file://`）时才回落到 `http://127.0.0.1:8760`。

### 多人同时玩：连接池

一条 keep-alive 连接一次只能跑一个请求。原来所有人都共用一条连接 + 一把锁，
两个人就得排队——一个人的决策要等另一个人的往返。现在开了 **4 个连接的池子**，
每个槽位各持一条连接，互不阻塞：

```
三个客户端同时打，各 6 板：
  玩家1 中位 389ms · 玩家2 中位 348ms · 玩家3 中位 370ms
  18 次决策总墙钟 2.6s（串行会是 7.0s）⇒ 并行加速比 2.6×
```

每人每板的中位耗时和单人是同一个数（~360ms），说明**没有互相排队**。

顺带把计时改成**线程局部**：并发时 `LAST_CALL_TIMING` 这种全局变量会被别的请求覆盖，
日志里的「建连 / 模型」拆分就会错乱。决策记录里也加上了 `peer`，能分清是谁在玩。

### 安全边界（说清楚）

- JEV 的 API key **只存在于 server.py 进程里**，页面里没有任何凭据，抓包也拿不到。
- 但**谁能打开这个地址，谁就能花你的 token**。这是个没有认证的中继——
  自己家/自己办公室的局域网没问题，**别往公网上放**。
- 每次决策约 918 输入 token ≈ $0.00004。别人玩一局（几百次决策）大约几美分。

## 那 340ms 到底花在哪：一条一条量出来的

结论先放：**Jev 自己只想 24ms，其余全是网络。** 证据是同一条热连接上交替发两种请求：

| 请求 | 中位 | 说明 |
|---|---|---|
| 极小请求（304 tok，一个选择题） | **313ms** | ≈ 纯网络往返 + 服务端固定开销 |
| 完整请求（918 tok，真实局面） | **337ms** | 只多 **24ms** |

```
每一步 ~337ms
├─ ~200ms  网络一次往返（走代理到 AWS 美西俄勒冈）
├─ ~110ms  API 服务端自己的固定开销
├─  ~24ms  ← Jev 真的在算这 918 tok 的局面
└─   ~1ms  本地中继
```

### 是代理的锅吗？——是主要项，但已经接近物理下限

这台机器上 `api.typesafe.ai` 解析到 **198.18.13.179**，属于 **198.18.0.0/15 fake-IP 段**，
系统里挂着多个 utun 接口 ⇒ 流量**全部**走本地代理（Clash/Surge 那类 TUN 模式），
那个 2~16ms 的「TCP 连接」只是连到本机假 IP。

同一路径上量不同站点：

| 站点 | TLS 握手 | 首字节 |
|---|---|---|
| 百度（规则直连） | 43~49ms | 7~9ms |
| cloudflare（走代理） | 328~663ms | 89~107ms |
| api.typesafe.ai（走代理） | 425~600ms | 200~223ms |

代理出口在**香港**（cloudflare trace 的 `colo=HKG · loc=HK`），而 API 源站是
**AWS 美西俄勒冈** ⇒ 香港到俄勒冈的跨太平洋往返本来就有 ~170ms。
**所以那 ~200ms 大部分是物理距离，不是代理没选好**；换个美西出口最多省 20~40ms。

（试过直连真实 IP 44.227.31.201：TUN 模式下照样被接管，依旧是 0.46s/0.67s，绕不开。）

### 换出口实测：亚洲 vs 美国（同一个 API、同一套测法）

出口位置靠 cloudflare trace 的 `colo` 字段定位。两次都是**热连接**上跑，直接可比：

| | 香港出口 colo=HKG | 美国出口 colo=LAX |
|---|---|---|
| 极小请求（= 网络+服务端地板） | **313ms** | 339ms |
| 完整请求（真实决策） | **337ms** | 363ms |
| 连续 45 次的中位 / p90 / 最慢 | — | **365 / 428 / 489ms** |
| 同一窗口的 30 次里慢于 1s | 10 次（含 8.8s / 7.0s / 9.9s） | **0 次** |
| 新建连接的隧道探测（每次都是新握手） | 324~390ms | **930~1010ms** |

**结论：美国节点每步慢约 26~28ms（约 8%，手感上分辨不出），但尾巴干净得多。**
比赛里让人接不到球的正是尾巴（一次 9s 的决策等于这一板没救），所以**美国节点更合适**。

一个反直觉的细节：美国节点的**新建连接**贵得多（隧道探测 970ms vs 390ms）——
跨太平洋路径的握手要多走好几个往返。所以 keep-alive 在美国节点上更值钱：
一旦掉线重连，那一下要 ~790ms。

（同样是间歇性的：美国节点跑 45 次一次没慢，看门狗那边仍偶发一次 1308ms。
这类抖动两个节点都可能碰上，跟节点选择没有必然关系。）

### 所以真正能优化的只有「握手次数」

- **每次重新建连要走一次 TLS 握手 ≈ 450~600ms**（香港出口 + 跨太平洋）。
  旧代码每次都重连 ⇒ 每步 1.4~2.4s。改成 keep-alive 复用后 ⇒ 每步 ~340ms。**这是唯一的大头，已经拿下。**
- 每步只剩 **1 个往返**，没有第二次可省；请求流水线只会把 token 成本翻倍。

```bash
python3 /tmp/jev_split.py        # 极小 vs 完整请求：24ms 那个差值就是这么量出来的
python3 /tmp/jev_where_slow.py   # 慢的时候到底是隧道还是 API（会换新连接重试同一请求）
python3 /tmp/jev_watchdog.py     # 蹲 8~10s 抖动：慢的那一刻同时记录隧道耗时
```

## 延迟：一张实测账单

**同一次运行、单线程、每 2.5 秒一次、30 次真实调用**（`/jev/log` 里逐条可查）：

```
1308(重连) 894 427 369 389 422 574 2470 2223 8777 6986 9873 375 775 1062 1979 412 1231 712 543 1835(重连) 268 311 297 298 367 292 406 353 525
```

| | |
|---|---|
| 模型本身中位 | **427ms** |
| 但方差极大 | 好的一段稳定在 **268~574ms**；差的一段连着跑出 **8.8s / 7.0s / 9.9s** |
| 中继自己的开销 | 可忽略（`http.client` 直接把响应交出去，没有中间层）|
| 重新握手 | 30 次里 2 次，每次多花 **0.9~1.5s**（长连接能扛住 ≥120s 空档，所以不常见）|

**结论：延迟的大头不在本地，而在对方自己的抖动。** 本地这边已经把能做的做完了：

- 长连接复用（原来每次都要重做一次 TLS 握手，那是 1.4~2.4s 的主要来源，已修）
- **TLS 会话复用（重新握手时省时间）测过但没做**：恢复握手 427~672ms vs 完整握手 637~1130ms，
  而重连只占 2/30 ⇒ 平均只省约 20ms。**不值得为它加复杂度。**
- 请求不流水线（同一时刻只问一个）：飞行 2.9s、每次 0.43s，闭环已经能问 5 次，
  pipelining 会把成本翻倍换来有限的收益。

### 看门狗抓到的东西：陈旧连接

204 次决策里慢于 2s 的有 5 次。逐条看它们的构成：

```
决策 4104ms · 隧道 1169ms · 换新连接重试同一请求  341ms   ⇒ 陈旧连接
决策 2274ms · 隧道  978ms · 换新连接重试同一请求  398ms   ⇒ 陈旧连接
决策 5215ms · 隧道  916ms · 换新连接重试同一请求 1291ms   ⇒ API 自己
决策 2003ms · 隧道  945ms · 换新连接重试同一请求 1007ms   ⇒ API 自己
```

**前两次里那 4 秒纯粹是白等**：keep-alive 连接被隧道悄悄掐了，但客户端不知道，
于是一直等；换条新连接，同一个请求 341ms 就回来了。

修法是一道读超时：第一趟只等 **1.2s**（正常中位 0.35s、p90 0.66s，1.2s 远在正常范围之外），
超了就关掉重连、换新连接再来一次（最多三趟）。

```bash
python3 /tmp/jev_stale_test.py   # 撑住 30 秒的假服务端 ⇒ 整体 1203ms 返回（对照：不修是 30004ms）
```

两个坑记在这里，免得下次再踩：

- `HTTPConnection.timeout` **只在建 socket 那一刻生效**，之后改它没用，要 `sock.settimeout()`。
- 但**连接**超时不能也用 1.2s：TLS 握手本身就要 0.8~1s。所以只调**读**超时。

### 两个由此暴露出来的真 bug（已修）

1. **超时被当成掉线**。实测对方能慢到 9.9s，而截止时间是 9s ⇒ 一旦超时，原来的代码把
   `online` 置为 false，**拍子会当场冻住**。超时 ≠ 掉线：现在超时只是把这次决策作废、
   **拍子沿着上一个指令继续执行**，并在日志里记一条「慢掉」。掉线判定只留给「真的连不上」。
2. **HUD 把离群值当常态**。表头原来显示**上一次**的毫秒数，于是对方偶尔抖一下，
   屏幕上就一直挂着 `1454ms`。现在显示**滑动平均**（它的典型反应速度），
   只有明显离群时才附注「上次 X ms」。单次的精确值与构成在决策日志里逐条可查。

```bash
jsc /tmp/jev_timeout_test.js   # 卡住不冻住、不误判掉线、慢决策会计数
python3 /tmp/jev_idle_probe.py # 长连接能撑多久（实测 ≥120s 空档仍复用）
```

## 球速上限：332 → 551 px/s（+66%）

上限不是拍脑袋定的，而是从【实测的决策延迟】倒推出来的：

```
单程飞行必须容得下：
   最后一次决策的往返          0.55s   ← 实测中位 0.36s、p90 约 0.5s，留了余量
 + 机械臂横跨全台              0.42s
 + 至少还有一次修正的机会      0.55s
 + 安全余量                    0.23s
 ────────────────────────────────────
 = 1.75s   ⇒  964 / 1.75 = 551 px/s
```

早期版本这里是 `2.0 + 0.9 = 2.9` ⇒ 上限只有 **332**，那个 2.0s 是**长连接还没修好时**的延迟。
修好之后实测降到了 0.36s，所以这个常数早该跟着改。

| | 旧 | 新 |
|---|---|---|
| 上限 | 332 px/s | **551 px/s** |
| 发球起步 | 上限的 62% = 206 | 上限的 **70%** = 386 |
| 台阶 | 206→226→249→274→301→332（5 板） | **386→424→467→513→551（4 板）** |
| 单程飞行 | 2.90s | 1.75s |
| 一板球里可决策 | 5 次 | 3 次 |

**代价是诚实要说的：一板球里它能算的次数从 5 次降到 3 次**——它变快的同时也变笨了一点。
这是速度与决策质量之间的直接交换，`--cap N` 就是这个旋钮：

```bash
python3 server.py --cap 450     # 4 次决策，比 551 稳一点
python3 server.py --cap 700     # 只有 2 次决策，接不住别怪我
```

顺带修掉一个隐藏的夹子：原来是 `min(BASE_SPEED, 上限) × 起步比例`，
上限一旦超过 `BASE_SPEED(430) `，起步就变成 430×0.70=301，爬满上限要 9 板。
现在是 `上限 × 起步比例`，4 板到顶。

**你的球拍完全跟得上**：`HUMAN_MOUSE_SPEED = 2600 px/s`，0.37 秒就能横跨全台。

## 球速：一板球里逐渐加速，撞到上限为止

球速不是常数，而是**每被球拍击一次就涨一档**，涨到上限就不再涨（撞墙不加速）：

```
发球 206  →  226  →  249  →  274  →  301  →  332（上限，之后一直 332）
       ×1.10   ×1.10   ×1.10   ×1.10   ×1.10
```

- **起步 = 上限的 62%**，每板 **×1.10** ⇒ 约 5 板追到上限，正好在一场普通回合（3~5 板）里就能感觉到越打越快
- **撞墙不加速**，只有球拍击球才加速（这条规则写在提示词里告诉它，连同当前球速和上限）
- HUD 底部的球速条走的就是「当前球速 / 上限」，可以直接看着它一档一档涨上去

### 上限是「它还来不来得及算」的那条线

上限不是拍脑袋定的。要它来得及，球飞半场的时间必须大于「决策耗时 + 机械臂横跨全台的耗时」：

| 球速上限 | 单程飞行 | 余量 | 预期可决策次数 | 结论 |
|---|---|---|---|---|
| **332（默认）** | 2.90s | 2.03s | 5 | 来得及，很宽松 |
| 450 | 2.14s | 1.27s | 3 | 来得及 |
| 550 | 1.75s | 0.88s | 2 | 来得及，偏紧 |
| 700 | 1.38s | 0.51s | 2 | 勉强 |
| 900 | 1.07s | 0.20s | 1 | 边界 |
| 1180 | 0.82s | −0.05s | 1 | **来不及** |

（实测决策中位 0.45s、机械臂横跨全台 0.42s。启动横幅会把当前上限的这行算术打出来。）

```bash
python3 server.py --cap 550        # 换一个更快的上限（默认 332）
```

上限是**固定常数**而不是跟着实测延迟变的——否则「测试条件」会被被测对象自己的延迟改动。
但它也不是魔法数字：算的是上面那条不等式，`--cap` 就是那个旋钮。

```bash
jsc /tmp/jev_speed_test.js   # 加速测试：起步 62%、每档 ×1.10、只涨不跌、永不超过上限
```

## 待命位：球不在飞来时，它站哪儿等

球被回过去之后，本地不再让拍子停在"上次击球的位置"——而是**问它一次**：
`kind:"ready"`，同样的 7 个候选拍位，问的是"为了两头都来得及，你要站哪等"。

实测（真实 API，6 个不同球况）：

| 问法 | 它选正中心 | 它选最边上 |
|---|---|---|
| "你想站哪等？"（开放） | 2/6 | 2/6 —— 球往下飞它就站下面 |
| **"为了两头都来得及，你要站哪等？"** | **6/6** | 0/6 |

**是我把问法改得更贴目标才拿到 6/6 的**，这一点必须说明：开放问法下它在"追着球站"，
而不是"守着台面站"。改成战术目标问法后它稳定选正中心——这仍然完全是它的决定，
本地没有规定中心，只是把问题问对了。

```bash
jsc /tmp/jev_ready_test.js    # 待命位测试：会问、走得到中心、且绝不覆盖拦截目标
python3 /tmp/jev_ready_probe.py  # 真实 API：6 个球况下它选哪等
```

## 玩法

- **你在左边，JEV 在右边**；每局 11 分、10:10 后净胜 2 分且每分换发球，先赢 3 局者胜。
- 击球点越靠拍边角度越大，跟拍加转，每次回球球速 ×1.10（上限即上面的球速上限，起步放在上限的 70%）。
- 档位只改 JEV 的**机械限速**和人格提示词（轻松 520 / 普通 900 / 困难 1500 / 宗师 2600 px/s）。

| 操作 | 按键 |
| --- | --- |
| 移动球拍 | 鼠标移动 / 触屏拖动 / `W` `S` / `↑` `↓` |
| 暂停、继续 | `空格` |
| 重新开始 | `R` |
| 切换档位 | `1` `2` `3` `4` |
| 静音 | `M` |
| 显示/隐藏模型指令线 | `P` |
| 打开/关闭 JEV 决策日志 | `L`（或点右下角按钮） |

HUD 上能看到模型的思考：指令线旁的 `模型指定落点 y=367 [b4 26%]` 是它对落点的判断和置信度，
底部 `延迟 0.4s · 球速上限 551 px/s` 是它把比赛拖慢了多少，
`落点判断 7/12 · $0.0006 · 12000 tok` 是本场累计命中率与花费。
想看每一次的完整过程，点右下角 `▤ JEV 决策日志`。

## 成本

实测 **905 输入 token/次**（pure 一问）/ **996**（assisted 三问），官方价 $42/Btok 输入、输出免费
⇒ **约 $0.00004 每次决策**。

开了闭环多判之后，一板球约问 5 次，成本按同样倍数涨：

| | 每次决策 | 一板球（~5 判） | 一局 11 分（约 60 板）|
|---|---|---|---|
| 花费 | $0.00004 | $0.0002 | **约 1 分钱** |

也就是说多判把成本乘了 5 倍，但绝对量仍然可以忽略。决策日志里实时累计 token 和花费，
按 `L` 就能看到这局到底花了多少。

## 本地还剩什么

**pure 模式（默认）下，本地只剩三样，且都不做任何"决定"：**

- **查表**：它选 `p4` → 指令 `move_to = 391.8`。恒等映射，一位小数都不动
  （`/tmp` 的纯模式检查里有一条断言逐位验证 7 个标签都不许被改动）。
- **伺服**：`jev.y += clamp(goal - jev.y, ±limit·dt)`，四行。没有预测、瞄准、走位策略。
- **物理**：球的运动、碰撞、反弹、计分、过热降速。这些是台面规则，并且**全部写进状态告诉它**
  （`max_bounce_angle_rad` / `speedup_per_hit` / `max_ball_speed` / `machine_heat`），
  所以"算球会飞到哪里"这件活是它自己的，本地不给答案。

assisted 模式在这之上多了三处换算：到达时间、区间中点、`aim`/`power` 的映射。

**掉线时 JEV 会站着不动**，并在 HUD 和菜单上明确报错——不会偷偷切回本地算法代打。
（实测：中继一死，拍心 0 帧移动；让模型故意站到球的反方向，10 个球 0 个接住。）

### 怎么自己验证"没有本地算法"

```bash
python3 /tmp/jev_pure_test.py     # 14 项机械检查：无到达时间、无泄漏、查表恒等、选项不被钳位…
python3 jev_eval_compare.py       # 四种问法的横向对比（含 API 是否支持数字输出的探测）
python3 jev_eval_pure.py          # 纯驱动问法单独测
```

`jev_eval_*.py` 里有一条 `assert_no_leak()`：真实到达点一旦出现在发给模型的文字里，
脚本会直接报错退出。**分数是用本地算的真相打的，但那个真相一个字都不进提示词**——
这是"用本地算分"和"用本地帮助它"的区别。

## 安全

- **key 只存在于 `server.py` 进程**（`jev.config.json` 或环境变量），不会进入网页、不会进浏览器。
- `jev.config.json` 已加入 `.gitignore`，且**不会通过 HTTP 暴露**（静态资源白名单，实测 404）。
- 中继默认只监听 `127.0.0.1`；静态响应不带 CORS 头。
- 大脑接口校验 `Origin`：只接受同源页面、`file://` 打开的页面和本机页面，其他网站一律 403。

## 开发工具

| 脚本 | 用途 |
| --- | --- |
| `jev_probe.py` | 用一个球考 Jev 的落点命中率/延迟/置信度 |
| `jev_probe_ask.py` | 对比三种问法（七选一 / 独立 noul / 三段）哪个准 |
| `jev_latency.py` | 延迟归因：是状态太长还是问题太多在拖慢它 |
| `python3 server.py --mock-brain` | 测试替身：不调用模型，只验证链路（调试用） |

改调参的地方：`SYSTEM_PROMPT` 不存在了——Jev 的「世界观」在 `server.py` 的 `describe()`（它看到什么）
和 `build_questions()`（问它什么）；球速的让步在 `index.html` 的 `TEMPO_*` 常量；
机械臂上限与过热在 `LEVELS[].speedLimit` / `HEAT_*`。

## 「它怎么老是接不到球」——一次完整归因

现象：回球成功率明显偏低。下面全部来自**真实对局的复盘**，不是模拟。

### 第一步：把中继里的决策日志拿来复盘（不花一分钱）

中继存了每条决策的**完整球状态**和**它的答案**，所以可以离线算「真实到达点」，逐条比对：

```
232 次「球飞来」的决策：
  |答案 − 真实到达点|  中位 47px · 平均 59px · p90 132px     （拍子半高 56px 才够得着）
  够得着 123/232 = 53%
```

**一半以上的决策，物理上就够不着**——不是它没执行，是位置差了 56px 以上。

### 第二步：到底是哪一类球错得多

```
0 次反弹（直线球）  164 次 · |误差|中位 43px · 够得着 58%
1 次反弹            62 次 · |误差|中位 85px · 够得着 39%
```

反弹球确实更差。但**偏差不在这里**：把「带符号误差」拆开看，直线球偏上 81 次 / 偏下 33 次
（中位 -19px），要反弹的球反而没有明显偏向。于是验一下「它是不是干脆没算反弹」——
把「按直线飞出去、被台边截断」当假设去拟合：**只有 12% 的样本符合，假设否掉。**

### 第三步：混淆矩阵——问题出在「差一档」上

```
7 个可选拍位：179 232 285 338 392 445 498
档距 53.3px  ·  拍子半高 56px      ← 两者几乎一样大
选中正确档 83/232 = 36% · 差一档以内 84%
偏移分布：−1档 79次 · 0档 83次 · +1档 34次
```

**它 84% 的时候都在一档之内。但「差一档」= 53.3px，而拍子只容得下 56px——差一档几乎必死。**
所以这不是「模型笨」，是**档距和拍高被设成了同一个数量级**。

### 第四步：两个「看起来能救」的方向，实测都不行

**改提前量**（`REASK_LEAD_S`，答完到球到达之间留多少时间）——拿同一批数据做反事实推演：

| 提前量 | 最后那次答案的\|误差\|中位 | 够得着 |
|---|---|---|
| 0.12s（现状） | 43px | 56% |
| 0.28s | 45px | 54% |
| 0.42s | 49px | 52% |

**没用。** 答得早一点、晚一点，误差几乎不变（它错得是稳定的，不是随机的）。

**改问法**——拿 116 个真实局面离线重放，每种问法各问一次真模型：

| 问法 | \|误差\|中位 | 够得着(56px) | 够得着(78px) |
|---|---|---|---|
| A 现状（7 档 · 坐标标签） | **58px** | 49% | 78% |
| B 7 档 · 序数标签（topmost…bottom-most） | 67px | 45% | 74% |
| C 9 档 · 序数标签 | 69px | 41% | 74% |

**改标签、改档数都没用，甚至更差。这个精度是内在的。**

### 第五步：一个错误结论（留在这里当反面教材）

按上面的误差分布列一张表，很容易得出「把拍子加大就行了」：

| 拍子半高 | 能接住 |
|---|---|
| 56px（原来） | 53% |
| 80px | 78% |
| 100px | 82% |

**这个方向是错的，而且错得很彻底。** 拍子的半高就是**容错**，也就是考卷的评分标准：

- 容错是全屏 ⇒ 回球率 100% ⇒ **这个数字里一点模型信息都没有**。
- 回球率会变成「拍子多大」的函数，而不是「模型多准」的函数。
- 而且它是**不可证伪**的：任何模型、任何乱猜，加大拍子都能让指标变好看。

`T` 键保留下来了，但它只是**对照开关**（看同一批决策在不同容错下会怎样），
默认值就是标准 112px，不许拿它当解法。

### 第六步：真杠杆——让它「心算的距离」变短

回头看误差是怎么来的。对每条真实决策算一件事：**它需要在 y 轴上往前推多远**
（= 到达点 − 球的当前位置），再和误差比对：

```
它要心算的 y 距离     次数    |误差|中位    够得着(56px)
  0 ~ 50px           52       23px        69%
  50 ~ 100px         55       41px        75%
  100 ~ 200px       109       69px        40%
  > 200px            16      146px        12%

拟合：|误差| ≈ 16.8 + 0.421 ×（要心算的距离）      相关系数 r = 0.57
```

按球的纵向速度分组，结论一样：

```
|vy| < 100 → 误差 37px · 够得着 77%
|vy| > 300 → 误差 116px · 够得着 28%
```

**它的误差几乎就是「心算距离」的函数。** 它不是不会做决定，是做不了
「|vy| × 飞行时间」这种多步心算——和当初「40 档分解 0/10」是同一个毛病。

而且这里有个正反馈：它自己偏 47px（拍半高才 56px）⇒ 击球点偏 ⇒ 回球角度陡 ⇒
下一板要心算的距离更大 ⇒ 更容易偏。**陡球是被它自己的误差喂出来的。**

于是动的是**击球角度上限**（`MAX_BOUNCE`，球变平），容错一动不动：

| 角度上限 | 要心算的距离 | 预计误差 | 预计够得着(56px 不动) |
|---|---|---|---|
| 0.92（原值） | 100% | 62px | 31% |
| **0.55（现在的默认）** | 60% | 44px | **74%** |
| 0.35 | 38% | 37px | 83% |

（用上面那条拟合线在 232 条真实决策上外推。`B` 键可切。）

**和加大拍子的区别**：容错始终是 56px，考卷没动；改的是题目难度。
而且**可证伪**——下次真实对局里误差如果没降到 40px 附近，就是我这个改法失败。
参考：真实乒乓球回球角度大多在 0.2~0.5 rad。

### 顺带修掉一个真 bug

提示词告诉模型「边缘最大角度 **0.41 rad**」，而代码里实际是 **0.92 rad**——
写成了 `0.45 × 真实值`，差了 2.2 倍。也就是说它一直在按错的物理算角度。
（这不影响「落点在哪」这一问，但它解释了为什么模型对回球角度的判断系统性偏乐观。）
现在提示词直接取状态里的 `max_bounce_angle_rad`，改角度它自动跟着变，
`tests/jev_pure_test.py` 加了 2 条检查锁死这个一致性。

## 日志里那些数字，谁说了算

有人指着决策日志问「怎么还有方向和发力，是不是还有别的维度」。查完的结果是：
**那两个是死字段，纯驱动模式下根本没问过。** 顺手把日志的显示 bug 修了。

一次决策中继只问 **一个问题**（`输入 1386 字 / 1 问` 里那个「1 问」），按情况分三种：

| 问题 | 什么时候问 | 选项 | 它答的就是 |
|---|---|---|---|
| `place` | 球飞向它 | 7 档 | 拍心该在哪个 y（**标签直接就是那个像素值**） |
| `ready` | 球飞回你那边、它在等 | 7 档 | 站哪等（只改等位，不改这一板的拦截点） |
| `serve` | 它发球 | 3 档 | 发球弧度 |

一次决策发出去的状态里，每个数字的归属：

| 日志上看到的 | 谁定的 |
|---|---|
| `#87` `18:56:54` `第 1 局 2:0` `回合 24` | 本地记的，跟模型无关 |
| `455ms` | 本地量的往返耗时（含网络） |
| `917 tok` `$0.000039` | 本地数出来的输入 token × $42/Btok |
| `本板第 3 判` | 本地记的：这是这一板第几次问（多判模式下才有） |
| `x→y=232` | **模型答的**（它选的那个标签对应的像素值） |
| `15%` + 概率分布 | **模型答的**（它对自己这个选择的自评置信度） |
| `速度 810` | **本地物理**：`档位限速 × 回合热度`（回合越长限速越低），发给中继后原样回给前端 |
| `方向 ?` `发力 0%` | **两个死字段**，见下 |
| `输入 1386 字` | 本地量出来的提示词长度 |

### 修掉的两个问题

**1. `方向` / `发力` 是遗留字段，对物理零影响。**
`brain.aim` 全文只被赋值一次、从没被读过；`power` 只写进日志记录，连 `brain` 都没进。
回球角度完全由「击球点离拍心多远」机械决定（`tryHit` 里的 `rel`）。
纯驱动模式下中继**根本不问**这两个（返回里没有 `aim`/`power`），assisted 基线会问、
但答案不参与物理——也就是说 assisted 基线里有 2 个白花钱的问题。日志里已经不显示了，
代码里也注明了。**它不是「新维度」，是看着像能被它控制的假旋钮。**

**2. `ready` 那一问的日志渲染错了。**
`ready` 的返回里没有 `place`，日志渲染就掉进了兜底分支（assisted 的排版），
于是显示成 `? 0%  x→y=232  速度 810  方向 ?  发力 0%` ——看起来像「它没答」，
其实是「待命位」那一问，答案在 `recover_to` 里。现在 `ready` 有自己的渲染：`待命 p3 · 等位 y=338`。

> 所以纯驱动模式下，它一次决策**只决定一个数**：拍心该在哪个 y。
> 发球时是弧度，等球时是等位。没有别的维度。

## 球速还能再快吗：能，但每一档都在用它的准度换

先算清楚一件事：**提速对它的准度有没有帮助？没有。**

```
一趟飞行的 y 位移 = |vy| × (到拍面的距离 / vx)
                  = 到拍面的距离 × tan(击球角度)
```

球速同时出现在分子和分母上，**被约掉了**。也就是说：角度不变时，球变快不会让「要心算的距离」变长或变短。
提速唯一改变的是**它能有几次决策**，以及**最后一问发生在多远**。

### 一板里的每一问，精度不一样（真实对局 121 条）

| | 条数 | 误差中位 | 够得着 | 问的时候离拍面 | 还剩多久 |
|---|---|---|---|---|---|
| 第一问 | 28 | 37px | 71% | 776px | 1.42s |
| 中间各问 | 65 | 44px | 72% | 574px | 1.08s |
| **最后一问** | 28 | **25px** | **79%** | 378px | 0.70s |

**越晚问越准**（闭环是真的在起作用），而回球成败由**最后一问**决定。
最后一问发生在「剩余飞行时间 ≈ 它自己的延迟 + 余量 ≈ 0.47s」时，
所以它离拍面的距离 ≈ 0.47 × 球速 —— **球越快，最后一问被推得越远，误差越大**。

### 提速的代价表

| 球速上限 | 单程飞行 | 能决策几次 | 最后一问离拍面 | 误差中位 | 够得着 |
|---|---|---|---|---|---|
| **551（原来）** | 1.75s | **3 次** | 378px | 25px | 79% |
| 600 | 1.61s | 2 次 | 412px (+9%) | 27px | 79% |
| **650（当前默认）** | 1.48s | 2 次 | 446px (+18%) | 29px | 75% |
| 700 | 1.38s | 2 次 | 480px (+27%) | 32px | 71% |
| 800 | 1.21s | 2 次 | 549px (+45%) | 36px | 71% |
| 900 | 1.07s | **1 次** | 617px (+63%) | 41px | 71% |

（最后一问的误差按距离等比放大实测分布；够得着仍是 56px 半高，容错不动。）

**900 那一行要注意**：只剩 1 次决策，等于把闭环砍掉——那反而是「纯开环」的测法，
回球率会掉到第一问的水平（71%→ 更差）。想让它打得好，551 反而最优。

改球速：`python3 server.py --cap 650`（固定常数，不读实测延迟，所以各场次能横向比）。

## 换成本地 System One（laya-mlx）试了一次：**同一批局面，它比 JEV 差一半**

[mizorewww/laya-mlx](https://github.com/mizorewww/laya-mlx) 是同一路线的本地实现：
Apple Silicon 上用 MLX 跑 Laya typed-decision 模型，**7~14ms 一次决策、0 输出 token、不要 API**。
它的问题和我们的几乎一模一样（`type: choice` + `instructions` + `criteria` 标签→描述），
所以接进来**解码路径一行都不用改**：它选的标签仍然就是拍子要去的那个 y，本地依旧只有查表和伺服。

接法：中继加 `--brain laya`（默认仍是 `jev`）。用第二个端口起一份，两个大脑就能左右对比：

```bash
# 云端 JEV
python3 server.py --cap 650
# 本地 System One（要用装了 mlx 的 3.11+ 环境）
./.venv-laya/bin/python server.py --brain laya --port 8761 --cap 650
```

### 结果：同一批 232 个真实局面，容错都是半高 56px

| 大脑 | 误差中位 | 平均 | 够得着 | 延迟 | 最常选 |
|---|---|---|---|---|---|
| **JEV（云端）** | **47.5px** | 59.1px | **53%** | ~350ms | 铺得开 |
| laya 英文 421M（512 上下文） | 69.2px | 128.5px | 22% | 139ms | **p0 ×171（74%）** |
| laya 多语言 322M（1024） | 69.2px | 122.9px | 28% | 63ms | **p0 ×125** |
| laya typed-decisions 421M（1024） | 119.8px | 124.2px | 34% | 137ms | **p1 ×127, p2 ×99** |

**三个 checkpoint 全都明显不如 JEV，而且都会「塌缩」**——不论球在哪，它几乎总是选同一两档。
所以它的问题不是「算得不够准」，是**根本没在做空间外推这道题**。

孤立验证更直接：7 个「答案显而易见」的局面（球就在拍面上不动、球正好停在某个档位上）
只答对 4 个。连「球已经贴在你拍面上、完全不动 y=300」都答成 p1（231.9，偏 68px）。

> 这不是说这个项目不好——它是给「文本通类决策」做的（客服分诊那类），
> 300~400M 的编码器做「|vy| × 飞行时间」这种多步心算本来就不在它的能力面上。
> 但这条实验有价值：**评测仪第一次被用来评价第二个模型，而且给出了可比数字**。

想要「本地、免费、快」的话，代价是回球率从 53% 掉到 22~34%。
想让它准，还是得云端那个。

### 有意思的副产品

本地模型只要 ~70ms 一次决策，比 JEV 快 5 倍。如果哪天要把球速推到 900+，
用本地大脑反而能让一板球保住 3 次决策（见上面「球速」那一节：JEV 在 900 时只剩 1 次）。
也就是说：**换成它换来的是速度，代价是准度**——两者都可以量出来。

## 已知取舍

- **模型慢 = JEV 弱，而且球也会跟着变慢。** 想让它打得更快，换更低延迟的模型比换更聪明的模型有效得多。
- 延迟方差大（1.2~8.9s），滑动平均挡不住尾部，偶尔你会看到它明明判断对了却仍然够不着球。
